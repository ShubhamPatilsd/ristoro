{
  "name": "ristoro",
  "version": "1.0.1",
  "license": "MIT",
  "scripts": {
    "dev": "yarn dev:api & yarn dev:web",
    "dev:api": "cd api && source .venv/bin/activate && fastapi dev main.py",
    "dev:web": "cd web && yarn dev"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^23.1.0"
  }
}
